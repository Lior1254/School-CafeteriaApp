<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="75dp"
    android:background="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:layoutDirection="rtl"
    android:orientation="horizontal"
    android:paddingStart="16dp"
    android:paddingEnd="16dp">

    <androidx.appcompat.widget.AppCompatCheckBox
        android:id="@+id/cbAddonStatus"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:minWidth="48dp"
        android:minHeight="48dp"
        android:padding="4dp"
        android:text="@string/addon_default_name"
        android:textColor="@color/mc_text_primary"
        android:textSize="20sp"
        app:buttonCompat="@drawable/bg_circle_selector"
        app:buttonTint="@null" />

    <TextView
        android:id="@+id/tvAddonPrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/addon_default_price"
        android:textColor="@color/gray"
        android:textSize="20sp" />

</LinearLayout>
