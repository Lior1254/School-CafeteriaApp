<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- לעיגול מלא -->
    <style name="ShapeAppearanceOverlay.CircularImage" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <!-- לפינות מעוגלות (אפשר לשנות מ-16dp לערך שתרצה) -->
    <style name="ShapeAppearanceOverlay.Rounded20" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
    </style>


    <!-- ================================= -->
    <!--      Styles for Order Progress Bar    -->
    <!-- ================================= -->

    <style name="ProgressStepIcon" parent="Widget.AppCompat.TextView">
        <!-- הגדלת גודל העיגול -->
        <item name="android:layout_width">32dp</item>
        <item name="android:layout_height">32dp</item>
        <item name="android:background">@drawable/progress_icon_background</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/white</item>
        <!-- הגדלת גודל הטקסט/ה-V בתוך העיגול -->
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ProgressStepText" parent="Widget.AppCompat.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <!-- הגדלת המרווח מהעיגול -->
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="ProgressLine">
        <item name="android:layout_width">0dp</item>
        <!-- הגדלת עובי הקו -->
        <item name="android:layout_height">3dp</item>
        <item name="android:layout_marginHorizontal">4dp</item>
    </style>

</resources>